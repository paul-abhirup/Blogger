export const BACKEND_URL = "https://backend.abhiruppaul1249.workers.dev";
//as the backend is being deployed in the cloudflare workers
//so there is no need to start the backend locally at localhost:8787
